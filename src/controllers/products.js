export const AddProduct = async () =>{
    try {
        const {name, Qrcode, } = req.body
        if(!name || !Qrcode || !Manufacturer){
            return res
        }
    } catch (error) {
        
    }

}